# 기본: 텍스트는 LF로 통일 (서명/빌드가 LF 기준이므로 체크아웃 변환 금지)
* text=auto eol=lf

# 무결성 임계 source — entry hash는 이 파일의 checkout bytes에서 생성되므로 변환하지 않는다.
ui-shell/ui-shell.plugin.js         -text
www/main.js                         -text
kanidm-ca.crt                       -text
